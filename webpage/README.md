# Webpage

This folder contains the front end of the application. This application uses a `.env` file to store environment variables used in the app.

Create a `.env` file in this folder and write your API URI in the form of "VITE_API_BASE_URL=`my api url`" in the file.

To start the application, run the following commands:
- `npm install`
- `npm run dev`